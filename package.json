{
  "name": "lore",
  "displayName": "Lore — Enriched inline comments",
  "publisher": "annot8-lore",
  "version": "0.0.1",
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "lore.createEnrichedComment",
        "title": "Lore: Chronicle new lore"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -p ./ -w",
    "pretest": "pnpm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.79.0",
    "typescript": "^5.0.0"
  }
}
